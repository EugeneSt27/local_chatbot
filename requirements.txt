# Список необходимых библиотек для работы AI-ассистента
# Запустите `pip install -r requirements.txt`, если будете переносить на чистую машину в закрытом контуре.

openai>=1.0.0
streamlit>=1.30.0
vllm>=0.4.0
