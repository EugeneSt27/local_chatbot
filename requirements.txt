# Основной интерфейс
streamlit>=1.30.0
openai>=1.0.0

# Движитель vLLM 
# ПРИМЕЧАНИЕ: Нужен ТОЛЬКО если ENGINE="vllm" и у вас Linux.
# Если вы на Windows, vLLM не установится (используйте Ollama).
# vllm>=0.4.0 
